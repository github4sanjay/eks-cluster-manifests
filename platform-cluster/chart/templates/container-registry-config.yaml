apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-image-updater-config
data:
  registries.conf: |
    registries:
    - name: Jetbrain Space
      api_url: https://opiniongist.registry.jetbrains.space
      prefix: jetbrains.space
      credentials: secret:default/container-registry#credential